import { LuasPersegiPipe } from './luas-persegi.pipe';

describe('LuasPersegiPipe', () => {
  it('create an instance', () => {
    const pipe = new LuasPersegiPipe();
    expect(pipe).toBeTruthy();
  });
});
