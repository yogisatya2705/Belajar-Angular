import { RedColDirective } from './red-col.directive';

describe('RedColDirective', () => {
  it('should create an instance', () => {
    const directive = new RedColDirective();
    expect(directive).toBeTruthy();
  });
});
